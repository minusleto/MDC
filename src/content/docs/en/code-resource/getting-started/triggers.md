---
title: Triggers
description: "Conditions in scripts"
---

## What is a trigger

A trigger checks the current game state. Triggers are commonly used in `if`, `limit`, `trigger`, `available` and related blocks.

### Rule of thumb

Determine the scope first, then verify that the trigger exists for that object type. A trigger checks state; an effect changes state.
